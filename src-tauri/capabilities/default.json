{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability for the SmartDocs main window: core window/webview events only. No shell, no fs, no dialog, no http permissions are granted to page code — all privileged work happens in Rust.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
