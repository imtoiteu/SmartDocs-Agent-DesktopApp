{
  "name": "smartdocs-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "SmartDocs desktop shell — Tauri v2 + Python sidecar",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build:sidecar": "bash scripts/build-sidecar.sh",
    "build": "bash scripts/build-sidecar.sh && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
